<%= link_to "New Project", new_project_path %>
<h2> Projects </h2>
<table border=1>
  <ul>
    <% @projects.each do |project| %>
      <tr><td> <li><%= link_to project.name, project %> </li></td></tr>
    <% end %>
  </ul>
</table>
